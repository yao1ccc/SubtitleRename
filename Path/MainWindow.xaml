<Window
    x:Class="SubtitleRename.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:av="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="clr-namespace:SubtitleRename.ViewModels"
    Width="600"
    Height="800"
    Background="#FF1F1F1F"
    mc:Ignorable="av">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Styles/TextBoxDictionary.xaml" />
                <ResourceDictionary Source="/Styles/LoopButtonDictionary.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>
    <Window.DataContext>
        <vm:MainViewModel />
    </Window.DataContext>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="auto" />
        </Grid.RowDefinitions>
        <TextBox Grid.Row="0" Margin="5">
            <TextBox.Style>
                <Style BasedOn="{StaticResource {x:Type TextBox}}" TargetType="{x:Type TextBox}">
                    <Style.Triggers>
                        <Trigger Property="IsFocused" Value="True">
                            <Setter Property="Text" Value="{Binding RootFolder, Converter={x:Static vm:DirectoryInfoConverter.Instance}, Mode=OneWayToSource, ValidatesOnDataErrors=True, ValidatesOnExceptions=True}" />
                        </Trigger>
                        <Trigger Property="IsFocused" Value="False">
                            <Setter Property="Text" Value="{Binding RootFolder, Converter={x:Static vm:DirectoryNameConverter.Instance}, Mode=OneWay}" />
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </TextBox.Style>
        </TextBox>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="200" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition />
                <RowDefinition />
            </Grid.RowDefinitions>
            <TextBox
                Grid.Row="0"
                Grid.Column="0"
                Margin="5"
                Text="{Binding VideoFilter, Converter={x:Static vm:RegexConverter.Instance}, UpdateSourceTrigger=PropertyChanged, ValidatesOnExceptions=True}" />
            <TextBox
                Grid.Row="1"
                Grid.Column="0"
                Margin="5"
                Text="{Binding SubtitleFilter, Converter={x:Static vm:RegexConverter.Instance}, UpdateSourceTrigger=PropertyChanged, ValidatesOnExceptions=True}" />
            <StackPanel
                Grid.Row="0"
                Grid.Column="1"
                Orientation="Horizontal">
                <Button
                    Margin="0,0,5,0"
                    Content="⇦"
                    Style="{StaticResource LoopButtonStyle}" />
                <Button Content="⇨" Style="{StaticResource LoopButtonStyle}" />
            </StackPanel>
            <StackPanel
                Grid.Row="1"
                Grid.Column="1"
                Orientation="Horizontal">
                <Button
                    Margin="0,0,5,0"
                    Content="⇦"
                    Style="{StaticResource LoopButtonStyle}" />
                <Button Content="⇨" Style="{StaticResource LoopButtonStyle}" />
            </StackPanel>
            <TextBox
                Grid.Row="0"
                Grid.Column="2"
                Margin="5"
                Text="{Binding VideoSuffixes, Converter={x:Static vm:SplitConverter.Instance}, ValidatesOnDataErrors=True}" />
            <TextBox
                Grid.Row="1"
                Grid.Column="2"
                Margin="5"
                Text="{Binding SubtitleSuffixes, Converter={x:Static vm:SplitConverter.Instance}, ValidatesOnDataErrors=True}" />
        </Grid>
        <ItemsControl
            Grid.Row="2"
            Margin="5"
            FontSize="15"
            Foreground="White"
            ItemsSource="{Binding LINQBinding, Mode=OneWay}">
            <ItemsControl.Template>
                <ControlTemplate TargetType="{x:Type ItemsControl}">
                    <Border
                        Padding="1"
                        Background="#383838"
                        BorderBrush="#FFABADB3"
                        BorderThickness="1"
                        SnapsToDevicePixels="true">
                        <ScrollViewer Focusable="false">
                            <ItemsPresenter SnapsToDevicePixels="True" />
                        </ScrollViewer>
                    </Border>
                </ControlTemplate>
            </ItemsControl.Template>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <Ellipse
                            Width="15"
                            Height="15"
                            Margin="3,0"
                            Fill="LightBlue" />
                        <TextBlock vm:TextBlockHelper.RichText="{Binding}" />
                    </StackPanel>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
        <Button
            Grid.Row="3"
            Width="100"
            Height="40"
            Margin="5"
            HorizontalAlignment="Right"
            Content="转换"
            Foreground="White">
            <Button.Template>
                <ControlTemplate TargetType="{x:Type Button}">
                    <Border
                        x:Name="border"
                        Background="Green"
                        BorderBrush="#FFABAdB3"
                        BorderThickness="1">
                        <ContentPresenter
                            x:Name="contentPresenter"
                            Margin="1"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            Focusable="False"
                            RecognizesAccessKey="True"
                            SnapsToDevicePixels="True" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="true">
                            <Setter TargetName="border" Property="Background" Value="LightGreen" />
                        </Trigger>
                        <Trigger Property="IsPressed" Value="true">
                            <Setter TargetName="border" Property="Background" Value="Green" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Button.Template>
        </Button>
    </Grid>
</Window>
